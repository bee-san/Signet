"""Application entry point for Signet."""


def main() -> None:
    """Start Signet once the runtime assembly is configured."""
    raise SystemExit(
        "Signet is scaffolded in downstream-disabled mode; runtime assembly is not complete."
    )
